apiVersion: v1
kind: Service
metadata:
  name: agent-chat-app
  namespace: agent-chat
  labels:
    app: agent-chat-app
spec:
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 3000
  selector:
    app: agent-chat-app
