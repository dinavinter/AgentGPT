# from langchain import PromptTemplate
#
#
# scrape_prompt = PromptTemplate(
#     template="""Given the following overall objective `{goal}` enrich the provided link list by scraping the link pages and extracting the missing data available in JSON-LD or HTML "{list}"
#     input_variables=["goal", "list"],
# )
